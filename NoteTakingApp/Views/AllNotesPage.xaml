<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:NoteTakingApp.ViewModels"
             xmlns:models="clr-namespace:NoteTakingApp.Models"
             x:DataType="viewModels:AllNotesViewModel"
             x:Class="NoteTakingApp.Views.AllNotesPage"
             Title="My Notes"
             BackgroundColor="#F0F2F5">

    <Grid RowDefinitions="*,Auto">

        <CollectionView Grid.Row="0"
                        ItemsSource="{Binding Notes}"
                        SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Note">
                    <!-- Use a Border for the modern card view -->
                    <Border Stroke="#E0E0E0"
                            StrokeThickness="1"
                            StrokeShape="RoundRectangle 12"
                            BackgroundColor="White"
                            Padding="15"
                            Margin="10">
                        <Border.Shadow>
                            <Shadow Brush="Black" Opacity="0.05" Radius="10" Offset="0,4" />
                        </Border.Shadow>
                        <Grid RowDefinitions="Auto,Auto,Auto" RowSpacing="5">
                            <Label Grid.Row="0" Text="{Binding Filename}" FontSize="20" FontAttributes="Bold" TextColor="#1A1A1A" />
                            <Label Grid.Row="1" Text="{Binding Text}" MaxLines="2" LineBreakMode="TailTruncation" Margin="0,5,0,0" TextColor="#5A5A5A"/>
                            <StackLayout Grid.Row="2" Orientation="Horizontal" Spacing="10" Margin="0,10,0,0">
                                <!-- Use the page's BindingContext (the AllNotesViewModel) for commands -->
                                <Button Text="Edit"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.GoToNoteCommand}"
                                        CommandParameter="{Binding .}" />
                                <Button Text="Delete"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteNoteCommand}"
                                        CommandParameter="{Binding .}"
                                        BackgroundColor="Red" />
                            </StackLayout>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Grid.Row="1" Text="Add New Note" Margin="20" Command="{Binding GoToAddNoteCommand}"/>
    </Grid>
</ContentPage>